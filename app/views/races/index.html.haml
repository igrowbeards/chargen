%h1 Races

- @races.each do |race|
  = link_to race.name.titleize, race
  = link_to 'Edit', edit_race_path(race)
  = link_to 'Destroy', race, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Race', new_race_path
